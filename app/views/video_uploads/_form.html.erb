<%= form_with(model: video_upload) do |form| %>
  <% if video_upload.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video_upload.errors.count, "error") %> prohibited this video_upload from being saved:</h2>

      <ul>
        <% video_upload.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :caption, style: "display: block" %>
    <%= form.text_field :caption %>
  </div>

  <div>
    <%= form.label :video, style: "display: block" %>
    <%= form.text_field :video %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
